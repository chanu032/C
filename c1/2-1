#include <stdio.h>

int main()
{
    float x,y,area,length;

    printf("가로의 길이를 입력하시오:");
    scanf("%f",&x);

    printf("세로의 길이를 입력하시오:");
    scanf("%f",&y);

    area = x*y;
    printf("사각형의 넓이:%lf", area);

    length = 2*(x+y);
    printf("사각형의 둘레:%lf", length);

    return 0;
}
