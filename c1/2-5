#include <stdio.h>

int main()
{
    float x,y,z;
    float sum, aver;

    printf("3개의 실수를 입력하시오: ");
    scanf("%f %f %f",&x, &y, &z);

    sum = x+y+z;

    printf("합계=%f",sum);

    aver = sum/3;

    printf("평균=%f",aver);

    return 0;
}
